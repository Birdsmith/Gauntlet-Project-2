'use client'

import { Typography } from 'antd'

const { Title } = Typography

export default function TicketsPage() {
  return (
    <div>
      <Title level={2}>Tickets</Title>
      <p>Welcome to the tickets dashboard.</p>
    </div>
  )
} 